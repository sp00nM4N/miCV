name: Deploy Landing Page

on:
  push:
    branches: ["main"] # Se ejecuta cuando subes cambios a main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/actions/checkout@v4

      - name: Set up Node
        uses: actions/actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Setup Pages
        uses: actions/actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/actions/deploy-pages@v4
